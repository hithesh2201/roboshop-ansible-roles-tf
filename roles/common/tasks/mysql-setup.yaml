- name: install mysql client
  ansible.builtin.dnf:
    name: mysql
    state: present


- name: load cities data
  ansible.builtin.shell: "mysql -h {{MYSQL_HOST}} -uroot -p roboshop123  < /app/schema/shipping.sql"
